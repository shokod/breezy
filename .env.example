# Copy to .env or supply via docker-compose environment variables when running in Docker.
# Docker compose uses these values to build and start the container.
OPENWEATHER_API_KEY=your_key_here
NEXT_PUBLIC_APP_URL=http://localhost:3000
